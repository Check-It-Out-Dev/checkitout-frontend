# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: perf/demo-smoothness.spec.ts >> Demo smoothness >> the admin-ops tour advances promptly at every step
- Location: e2e-tests/perf/demo-smoothness.spec.ts:485:9

# Error details

```
Error: the tour moved the page under the visitor

expect(received).toBeLessThanOrEqual(expected)

Expected: <= 0.15
Received:    0.15434572053969475
```

# Page snapshot

```yaml
- generic [ref=e2]:
  - generic [ref=e4]:
    - generic [ref=e7]:
      - link "Check It Out" [ref=e9] [cursor=pointer]:
        - /url: /demo
        - generic [ref=e10]: Check It Out
      - navigation [ref=e11]:
        - link "Użytkownicy" [ref=e12] [cursor=pointer]:
          - /url: /user/list
          - img [ref=e13]: group
          - generic [ref=e14]: Użytkownicy
        - link "Odkrywaj" [ref=e15] [cursor=pointer]:
          - /url: /collaborations/list
          - img [ref=e16]: explore
          - generic [ref=e17]: Odkrywaj
        - link "Moje zgłoszenia" [ref=e18] [cursor=pointer]:
          - /url: /collaborations/registrations
          - img [ref=e19]: send
          - generic [ref=e20]: Moje zgłoszenia
        - link "Słownik" [ref=e21] [cursor=pointer]:
          - /url: /admin/dictionary
          - img [ref=e22]: menu_book
          - generic [ref=e23]: Słownik
        - link "Zgłoszenia wsparcia" [ref=e24] [cursor=pointer]:
          - /url: /support/admin/tickets
          - img [ref=e25]: support_agent
          - generic [ref=e26]: Zgłoszenia wsparcia
    - generic [ref=e28]:
      - banner [ref=e29]:
        - button "Zmień język" [ref=e30] [cursor=pointer]:
          - img [ref=e31]: expand_more
          - generic [ref=e32]: pl
        - button "Powiadomienia" [ref=e36] [cursor=pointer]:
          - img [ref=e37]: notifications
          - generic: "1"
        - button "Otwórz menu użytkownika" [ref=e40] [cursor=pointer]:
          - img [ref=e41]: account_circle
      - main [ref=e44]:
        - generic [ref=e46]:
          - generic [ref=e47]:
            - text: Znajdź odpowiednią współpracę dla swoich odbiorców.
            - heading "Przeglądaj kampanie" [level=1] [ref=e48]
          - generic [ref=e49]:
            - complementary [ref=e50]:
              - heading "Filtry" [level=2] [ref=e51]
              - generic [ref=e52]:
                - generic [ref=e55] [cursor=pointer]:
                  - generic [ref=e56]: Wynagrodzenie
                  - combobox "Wynagrodzenie" [ref=e58]:
                    - generic [ref=e59]:
                      - generic [ref=e61]: Wszystkie
                      - img [ref=e64]
                - generic [ref=e69]:
                  - generic [ref=e70]: Miasto
                  - textbox "Miasto" [ref=e72]
                - button "Zastosuj" [ref=e74] [cursor=pointer]:
                  - generic [ref=e75]: Zastosuj
                - button "Wyczyść" [ref=e78] [cursor=pointer]:
                  - generic [ref=e79]: Wyczyść
            - generic [ref=e82]:
              - list [ref=e83]:
                - listitem [ref=e84]:
                  - link "Letnia kampania specjałów kawowych Barter Demo Brand Sp. z o.o. Voucher degustacyjny dla dwóch osób + paczka kawy. Kraków" [ref=e85] [cursor=pointer]:
                    - /url: /collaborations/501
                    - generic [ref=e86]:
                      - heading "Letnia kampania specjałów kawowych" [level=2] [ref=e87]
                      - generic [ref=e88]: Barter
                    - paragraph [ref=e89]: Demo Brand Sp. z o.o.
                    - generic [ref=e90]:
                      - generic [ref=e91]:
                        - img [ref=e92]: payments
                        - definition [ref=e93]: Voucher degustacyjny dla dwóch osób + paczka kawy.
                      - generic [ref=e94]:
                        - img [ref=e95]: place
                        - definition [ref=e96]: Kraków
                - listitem [ref=e97]:
                  - link "Weekendowe testy sprzętu trekkingowego Barter Testowany zestaw zostaje u twórcy + relacja partnerska na sezon. Zakopane" [ref=e98] [cursor=pointer]:
                    - /url: /collaborations/503
                    - generic [ref=e99]:
                      - heading "Weekendowe testy sprzętu trekkingowego" [level=2] [ref=e100]
                      - generic [ref=e101]: Barter
                    - generic [ref=e102]:
                      - generic [ref=e103]:
                        - img [ref=e104]: payments
                        - definition [ref=e105]: Testowany zestaw zostaje u twórcy + relacja partnerska na sezon.
                      - generic [ref=e106]:
                        - img [ref=e107]: place
                        - definition [ref=e108]: Zakopane
              - group [ref=e109]:
                - generic [ref=e111]:
                  - generic [ref=e112]:
                    - generic [ref=e113]: "Wierszy na stronie:"
                    - combobox "Wierszy na stronie:" [ref=e118] [cursor=pointer]:
                      - generic [ref=e119]:
                        - generic [ref=e121]: "12"
                        - img [ref=e124]
                  - generic [ref=e127]:
                    - status [ref=e128]: 1–2 z 2
                    - button "Poprzednia strona" [disabled] [ref=e129]:
                      - img [ref=e130]
                    - button "Następna strona" [disabled] [ref=e134]:
                      - img [ref=e135]
  - generic [ref=e140]:
    - generic [ref=e141]:
      - generic [ref=e142]:
        - img [ref=e143]: check_circle
        - text: Gotowe
      - generic [ref=e144]: Operacje admina
    - generic [ref=e145]:
      - generic [ref=e146]:
        - img [ref=e147]: check
        - generic [ref=e148]: Odpowiedzi admina wątkują się na ticketach i powiadamiają użytkownika mailem.
      - generic [ref=e149]:
        - img [ref=e150]: check
        - generic [ref=e151]: Usunięcie to kaskada z preview — dokładne liczby per system, a samo usunięcie musi odesłać jednorazowy kod z tego preview, więc żaden ślepy DELETE nie przejdzie.
      - generic [ref=e152]:
        - img [ref=e153]: check
        - generic [ref=e154]: Każda kasacja zostawia zamaskowany ślad audytowy; dane znikają, dowód zostaje.
    - paragraph [ref=e155]: To był ostatni sandbox. Przeszedłeś całe demo — dzięki. Wróć do listy albo zajrzyj, jak to działa w środku.
    - generic [ref=e156]:
      - button "Zakończ — lista sandboxów" [ref=e157] [cursor=pointer]:
        - img [ref=e158]: flag
        - text: Zakończ — lista sandboxów
      - button "Jak to działa w środku" [ref=e159] [cursor=pointer]:
        - text: Jak to działa w środku
        - img [ref=e160]: menu_book
    - generic [ref=e161]:
      - button "Od nowa" [ref=e162] [cursor=pointer]:
        - img [ref=e163]: replay
        - text: Od nowa
      - button "Wszystkie sandboxy" [ref=e164] [cursor=pointer]:
        - img [ref=e165]: arrow_back
        - text: Wszystkie sandboxy
```

# Test source

```ts
  461 |     //
  462 |     // It is not the guide's work either. The longest script in the worst frame
  463 |     // is a rAF callback of which 102 of its 104 ms is `forcedStyleAndLayout` —
  464 |     // the ring measuring its control on the frame after the page replaced its
  465 |     // DOM. Caching the clip search moved it by nothing; removing the
  466 |     // fading-overlay opacity read entirely gave 267, 256, 265.
  467 |     //
  468 |     // So the worst frame is reported and not asserted. The three that remain are
  469 |     // the ones that describe answerability and they are stable: a regression
  470 |     // that costs every frame — the kind that once put 144 hit tests into a
  471 |     // single scroll — shows up as MORE long frames and more blocking, not as one
  472 |     // unlucky long one.
  473 |     const reading = `${String(seen.frames)} long frames, worst ${String(seen.worst)} ms, blamed on ${seen.blame}`;
  474 |     timing('long frames', reading, () => expect(seen.frames, reading).toBeLessThanOrEqual(8));
  475 |     timing('blocking', `${String(seen.blocking)} ms`, () =>
  476 |       expect(seen.blocking).toBeLessThanOrEqual(400),
  477 |     );
  478 |     // the "good" threshold for interaction latency
  479 |     timing('worst event', `${String(seen.worstEvent)} ms`, () =>
  480 |       expect(seen.worstEvent).toBeLessThanOrEqual(200),
  481 |     );
  482 |   });
  483 | 
  484 |   for (const key of TOURS) {
  485 |     test(`the ${key} tour advances promptly at every step`, async ({ page }) => {
  486 |       const errors: string[] = [];
  487 |       page.on('pageerror', (e) => errors.push(e.message));
  488 |       // Layout shifts within half a second of a click are excluded from CLS by
  489 |       // design — and the ring moves right after a click, so CLS is blind to
  490 |       // exactly the shifts this tour causes. Count them all.
  491 |       await page.addInitScript(() => {
  492 |         const w = window as unknown as Record<string, unknown>;
  493 |         const shifts: number[] = [];
  494 |         w['__shifts'] = shifts;
  495 |         new PerformanceObserver((list) => {
  496 |           for (const e of list.getEntries() as unknown as { value: number }[]) shifts.push(e.value);
  497 |         }).observe({ type: 'layout-shift', buffered: true });
  498 |       });
  499 |       await page.goto(`${BASE}/demo?start=${key}`, { waitUntil: 'networkidle' });
  500 | 
  501 |       for (let i = 0; i < 12; i++) {
  502 |         let before = await tourState(page);
  503 |         // a step that reloads the page needs a moment to come back
  504 |         for (let k = 0; k < 30 && !before.done && !before.pill && !before.panelNext; k++) {
  505 |           await page.waitForTimeout(100);
  506 |           before = await tourState(page);
  507 |         }
  508 |         if (before.done) break;
  509 |         expect(before.pill || before.panelNext, `step ${before.step} offers no way forward`).toBe(
  510 |           true,
  511 |         );
  512 | 
  513 |         // Once a way forward is offered it must stay that control. The ksef
  514 |         // beat used to hand the panel a Next while its simulator was still
  515 |         // opening, then swap it for the ring's pill — a button appearing and
  516 |         // vanishing under the visitor's cursor.
  517 |         await page.waitForTimeout(400);
  518 |         // a step that reloads may already be navigating; that is not a flicker
  519 |         const settled = await tourState(page).catch(() => null);
  520 |         if (settled && settled.step === before.step && !settled.done) {
  521 |           expect(settled.pill, `step ${before.step} changed its way forward`).toBe(before.pill);
  522 |         }
  523 | 
  524 |         // whenever the ring is up, it must be drawn around something real
  525 |         const ring = await ringOnTarget(page);
  526 |         if (ring && ring.drift !== null) {
  527 |           expect(
  528 |             ring.drift,
  529 |             `step ${before.step}: the ring is ${ring.drift} px off the nearest element (${ring.matched})`,
  530 |           ).toBeLessThanOrEqual(2);
  531 |         }
  532 | 
  533 |         const control = before.pill
  534 |           ? '[data-testid="guide-spot-next"]'
  535 |           : '[data-testid="guide-next"]';
  536 |         const started = Date.now();
  537 |         await page.click(control);
  538 |         await page.waitForFunction(
  539 |           (prev) => {
  540 |             const s = JSON.parse(sessionStorage.getItem('demoSandbox') ?? '{}') as {
  541 |               step?: number;
  542 |               done?: boolean;
  543 |             };
  544 |             return s.step !== prev.step || (s.done === true) !== prev.done;
  545 |           },
  546 |           { step: before.step, done: before.done },
  547 |           { timeout: 10_000 },
  548 |         );
  549 |         // the simulators have beats of their own; everything else is instant
  550 |         expect(Date.now() - started, `step ${before.step} took too long`).toBeLessThanOrEqual(1500);
  551 |         // the page is held while the guide types, and only then
  552 |         await expect(page.locator('[data-testid="guide-shield"]')).toHaveCount(0);
  553 |       }
  554 |       const shifted = await page.evaluate(() =>
  555 |         ((window as unknown as Record<string, unknown>)['__shifts'] as number[]).reduce(
  556 |           (n, v) => n + v,
  557 |           0,
  558 |         ),
  559 |       );
  560 |       // measured per tour: 0.006 to 0.085, and CLS reports as little as 0 of it
> 561 |       expect(shifted, 'the tour moved the page under the visitor').toBeLessThanOrEqual(0.15);
      |                                                                    ^ Error: the tour moved the page under the visitor
  562 |       expect(errors).toEqual([]);
  563 |     });
  564 |   }
  565 |   /**
  566 |    * The message's dissolve lives in the same keyframes as its flight because
  567 |    * the production pipeline once lost it. Angular scopes `@keyframes` names
  568 |    * per component; inside an `@media` block the minified build scoped only
  569 |    * the first name of a two-name `animation-name` list, so the dev server
  570 |    * ran both, the sandbox tier passed, and the live site left every message
  571 |    * lying on the pane. This runs against the BUILT app, so it sees what the
  572 |    * visitor sees: the first message is whole at some point and gone 1.3 s
  573 |    * after it appears.
  574 |    */
  575 |   test("the story's first message dissolves after landing, in the built app", async ({ page }) => {
  576 |     await page.goto(`${BASE}/`, { waitUntil: 'networkidle' });
  577 |     await page.locator('[data-testid="dashboard-mode-simulation"]').scrollIntoViewIfNeeded();
  578 |     await page.click('[data-testid="dashboard-mode-simulation"]');
  579 |     const cargo = page.locator('[data-testid="dashboard-flight"]');
  580 |     await cargo.waitFor();
  581 |     const seen = await cargo.evaluate(async (el) => {
  582 |       const read = () => Number(getComputedStyle(el).opacity);
  583 |       const t0 = performance.now();
  584 |       let peak = 0;
  585 |       while (performance.now() - t0 < 1300) {
  586 |         peak = Math.max(peak, read());
  587 |         await new Promise((r) => requestAnimationFrame(r));
  588 |       }
  589 |       return { peak, final: read() };
  590 |     });
  591 |     expect(seen.peak, 'the message never became visible').toBeGreaterThanOrEqual(0.95);
  592 |     expect(seen.final, 'the message is still lying on the pane').toBe(0);
  593 |   });
  594 | });
  595 | 
```