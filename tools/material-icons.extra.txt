# Icon names the scanner regexes cannot see (helper-function returns,
# names that may arrive from data). One name per line; # comments.
# fileIcon() in attachment rendering:
description
image
insert_drive_file
picture_as_pdf
# tech-stack-strip.component.ts: glyphs for chips without a brand mark (FALLBACK_GLYPH map)
translate
receipt_long
account_balance
